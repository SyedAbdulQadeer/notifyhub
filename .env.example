# Firebase Notification Relay Server Environment Variables

# Required: AES encryption key for decrypting Firebase service account configs
# This should be a secure random string that matches the key used by your Flutter client
SECRET_KEY=your_32_character_secret_key_here

# Optional: Server port (defaults to 3000)
PORT=3000
